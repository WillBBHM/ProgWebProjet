apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: servicebackend-ingress
  annotations:
#    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: servicebackend.info
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: servicebackend
                port:
                  number: 8080
